apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: datadog
  namespace: datadog
spec:
  values:
    agents:
      containers:
        agent:
          resources:
            requests:
              cpu: 400m
              memory: 256Mi
            limits:
              cpu: 400m
              memory: 256Mi
