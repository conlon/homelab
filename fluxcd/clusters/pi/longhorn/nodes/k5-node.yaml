---
apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: k5
  namespace: longhorn-system
spec:
  # Was temporarily false on 2026-09-11 while k5 drained after the k3 evacuation
  # overloaded it (load 23.75). Reverted once k5 settled back to <1.0.
  allowScheduling: true
  disks:
    default-disk-127143daf51e5136:
      allowScheduling: true
      diskType: filesystem
      path: /var/lib/longhorn/
      storageReserved: 73687573709  # 68.6 GiB reserved (30% of ~229 GiB)
      tags: []
  tags: []
