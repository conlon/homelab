---
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: longhorn-system
spec:
  selector:
    app: minio
  ports:
    - name: s3-api
      port: 9000
      targetPort: server-port
    - name: webui
      port: 9001
      targetPort: webui-port
